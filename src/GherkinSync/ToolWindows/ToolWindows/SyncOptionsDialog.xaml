<platform:DialogWindow  x:Class="GherkinSync.ToolWindows.SyncOptionsDialog"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                        xmlns:platform="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
                        xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
                        toolkit:Themes.UseVsTheme="True"
                        mc:Ignorable="d"
                        Title="GherkinSync: Synchronization options"
                        Width="800"
                        Height="600"
                        d:DesignHeight="600"
                        d:DesignWidth="800" 
                        DataContext="{Binding Mode=OneWay, RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*" />
            <ColumnDefinition Width="25*" />
            <ColumnDefinition Width="55*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Project" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox Name="ProjectName" Grid.ColumnSpan="2" Grid.Row="0" Grid.Column="1" Margin="5" Text="{Binding Path=SyncOptions.ProjectName, Mode=TwoWay}" />
        <Label Content="Test plan" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <TextBox Name="TestPlanId" Grid.Row="1" Grid.Column="1" Margin="5" Text="{Binding Path=SyncOptions.TestPlanId, Mode=TwoWay}"/>
        <TextBox Name="TestPlanName" Grid.Row="1" Grid.Column="2" Margin="0 5 5 5" Text="{Binding Path=SyncOptions.TestPlanName, Mode=TwoWay}"/>
        <Label Content="Test suite" Grid.Row="2" Grid.Column="0" Margin="5"/>
        <TextBox Name="TestSuiteId" Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding Path=SyncOptions.TestSuiteId, Mode=TwoWay}"/>
        <TextBox Name="TestSuiteName" Grid.Row="2" Grid.Column="2" Margin="0 5 5 5" Text="{Binding Path=SyncOptions.TestSuiteName, Mode=TwoWay}" />

        <Label Content="Description template" Grid.Row="3" Grid.Column="0" Margin="5"/>
        <TextBox Name="DescriptionTemplate" Grid.ColumnSpan="2" Grid.Row="3" Grid.Column="1" Margin="5" Text="{Binding Path=SyncOptions.DescriptionTemplate, Mode=TwoWay}"/>
        <Label Content="Custom fields" Grid.Row="4" Grid.Column="0" Margin="5" />
        <DataGrid Name="CustomFields" Grid.ColumnSpan="2" Grid.Row="4" Grid.Column="1" Height="200" Margin="5" ItemsSource="{Binding Path=SyncOptions.CustomFields, Mode=TwoWay}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Key" Width="200" Binding="{Binding FieldName}"/>
                <DataGridTextColumn Header="Value" Width="*" Binding="{Binding DefaultValue}" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2" Grid.Row="5" Grid.Column="1" HorizontalAlignment="Right">
            <Button Name="OkButton"  Content="Ok" Width="100" Margin="5" Click="OkButton_Click"/>
            <Button Name="CancelButton" Content="Cancel" Width="100" Margin="5" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</platform:DialogWindow>
